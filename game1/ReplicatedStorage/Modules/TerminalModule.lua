local TerminalModule = {}

-- Phase 2 placeholder: CLI parsing, command registry, and server validation hooks.
function TerminalModule.ParseCommand(commandString)
	-- Return structured command table or error message.
	return nil, "Terminal functionality unlocks in Phase 2."
end

return TerminalModule

